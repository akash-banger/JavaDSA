package TwodArrays;
public class TwoDArrays {
    public static void main(String arg[]){

        // creation of 2d arrays 
        // int matrix[][] = new int[4][4];
        // n*m: n are rows and m are coloumns 
        // int n = 3; 
        // int m = 4;

        // int n = matrix.length;
        // int m = matrix[0].length;

        // for(int i=0; i<n; i++){
        //     for(int j=0; j<m; j++){
        //         matrix[i][j] = sc.nextInt();
        //     }
        // }



        // there are two ways to store the 2d array in the memory
        // col major and row major
        // in row major the matrix will be stored as the rows.
        // means first row => second row => third row 
        // and memory address will go from left to right
        // similarly in col major. it will store at cols.

        // but in java: java do not use any of these 2;
        // in java . it is not necessay that our elements would store in the contiguous fasion

    }
}
